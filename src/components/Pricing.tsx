import { plans, type ServicePlan } from '../lib/payments'
export function Pricing({ onChoose }: { onChoose: (plan: ServicePlan) => void }) { return <section className="pricing" id="services"><p className="eyebrow">HUMAN EXPERTISE, WHEN YOU NEED IT</p><h2>From self-service to a real expert case.</h2><p className="pricing-intro">Free diagnostics remain useful. Paid support creates a private case for a qualified technician; availability and outcome depend on evidence and the physical device.</p><div className="plan-grid">{plans.map(plan => <article className="plan" key={plan.id}><p className="eyebrow">{plan.id === 'priority' ? 'PRIORITY' : 'ONE-TIME'}</p><h3>{plan.name}</h3><strong>{plan.price}</strong><p>{plan.description}</p><button className="primary wide" onClick={() => onChoose(plan)}>Open support case →</button></article>)}</div></section> }
